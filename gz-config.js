/* ==============================================================
   GENETIGZ — SITE CONFIG
   One place to point the whole frontend at your backend API.
   Change API_BASE once you deploy backend/ (see backend/README.md).
   Must be loaded before auth.js / login.js / signup.js / order.js.
============================================================== */
window.GZ_CONFIG = {
  API_BASE: 'http://localhost:5000/api'
};
